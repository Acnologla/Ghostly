class TileMap {
    constructor(name, tiles, layers, collision){
        this.name = name;
        this.tiles = tiles;
        this.layers = layers;
        this.collision = collision;
    }
}

export default TileMap;